extends page

block contents
  h1
    a(href=baseUrl + path)= "Posts tagged \"" + tag + "\""

  for post in posts
    .post-container
      h2.post-title
        a(href=baseUrl + post.path)= post.title
      p#post-date= post.date.toDateString()